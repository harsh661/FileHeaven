/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                hostname: "useful-seal-799.convex.cloud",
            }
        ]
    }
};

export default nextConfig;
